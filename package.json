{
  "name": "altimist-com-router",
  "version": "0.1.0",
  "description": "Cloudflare Worker that fronts the altimist.com zone end-to-end — dispatches /.well-known/* and /users/*/did.json to altimist-id, and proxies all other apex + subdomain traffic to the corporate-website-v2 rendering backend (Option D per ADR-013/ADR-014).",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no lint configured)\" && exit 0"
  },
  "dependencies": {
    "@altimist/did-publisher": "^0.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.8",
    "wrangler": "^4.97.0"
  }
}
